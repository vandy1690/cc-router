---
name: deep
description: "Run one task on Opus, the hard-reasoning tier, then return to the session's model. For architecture, root-cause debugging, dense logic, and larger multi-file changes."
argument-hint: "[task]"
model: opus
disable-model-invocation: true
---

Do this task now. It was sent to the deep-reasoning tier on purpose, so take the time the problem needs: find the root cause rather than the first plausible fix, weigh the real trade-offs, and verify the result before you report.

Task: $ARGUMENTS
